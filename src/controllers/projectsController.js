const { project } = require("../db");

const createProjects = async (description, title, image) => {};
const updateProjects = async (description, title, image) => {};
const getProjects = async () => {};

module.exports = { createProjects, updateProjects, getProjects };
